from mysupport.WindowContentMenuEx.configer import asconfig

@asconfig
def 添加配时文件():

    rulesfilename = "RULES.xlsx"

    return dict(locals())

@asconfig
def folder_limit():
    limit = 10
    dirname = "历史档案"
    matchlist = [r"^\d{4}-\d{2}-\d{2}.xlsx$"]
    return dict(locals())